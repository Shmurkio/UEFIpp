# Let Git normalize text files in the repository while preserving native
# Windows line endings in a Visual Studio checkout.
* text=auto

# Keep binary build products and symbol data out of text conversion.
*.efi binary
*.lib binary
*.pdb binary
